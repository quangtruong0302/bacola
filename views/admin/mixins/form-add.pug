mixin form-add(content)
    .card.mgr-10px
        .card-header
            | #{content}
        .card-body
            a.btn.btn-success.mgr-10px(href="#")
                i.fa-solid.fa-plus
                span #{content}