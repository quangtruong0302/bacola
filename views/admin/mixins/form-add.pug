mixin form-add(content)
    .card.mgr-10px
        .card-header
            | #{content}
        .card-body
            a.btn.btn-success(href="#")
                i.fa-solid.fa-plus
                span #{content}